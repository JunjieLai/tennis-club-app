# Development Environment Variables
# Copy this file to .env and fill in your values

# Server Configuration
PORT=5001
NODE_ENV=development

# PostgreSQL Database (Local Development)
# IMPORTANT: Changed from MySQL to PostgreSQL
# For local development, install PostgreSQL and create a database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tennisclub
DB_USER=postgres
DB_PASSWORD=your_postgres_password_here

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production_12345678
JWT_EXPIRE=7d

# Frontend URL (for CORS)
CLIENT_URL=http://localhost:3000

# ============================================
# SETUP INSTRUCTIONS:
# 1. Install PostgreSQL: brew install postgresql@14
# 2. Start PostgreSQL: brew services start postgresql@14
# 3. Create database: createdb tennisclub
# 4. Update DB_PASSWORD with your postgres password
# 5. Run: npm run init (to setup database and sample data)
# ============================================
