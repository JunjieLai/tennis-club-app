# Production Environment Variables (for Render deployment)
# Copy this file and fill in the values for production deployment

# Server Configuration
PORT=5001
NODE_ENV=production

# PostgreSQL Database (Supabase)
# Get these values from Supabase Dashboard > Settings > Database
DB_HOST=db.xxxxxxxxxxxxxx.supabase.co
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=your_supabase_database_password_here

# Alternative: Use DATABASE_URL (Supabase connection string)
# DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.xxx.supabase.co:5432/postgres

# JWT Configuration
JWT_SECRET=generate_a_random_256_bit_secret_key_here
JWT_EXPIRE=7d

# Frontend URL (Render Static Site URL)
CLIENT_URL=https://your-frontend-app.onrender.com

# CORS Origins (optional, defaults to CLIENT_URL)
# ALLOWED_ORIGINS=https://your-frontend-app.onrender.com

# ============================================
# HOW TO GENERATE JWT_SECRET:
# Run this in terminal: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Or use: https://generate-secret.vercel.app/32
# ============================================
